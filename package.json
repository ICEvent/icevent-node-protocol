{
  "name": "icevent-node-protocol",
  "version": "0.1.0",
  "description": "Universal Node Protocol for ICP",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "tsx": "^4.20.6",
    "typescript": "^5.8.3"
  }
}
